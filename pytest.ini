[pytest]
testpaths = tests
addopts = -q
markers =
    realdata: exercises the large local files in testdata/ (skipped when absent)
    embedding: runs the real BAAI/bge-small-en-v1.5 model (skipped without fastembed)
